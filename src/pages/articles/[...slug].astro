---
import MainLayout from "../../layout/MainLayout.astro";
import Layout from "../../layout/layout.astro";
import Tags from "../../components/Tags.astro";
import { formatDate } from "../../utils";
import { getEntry, render } from "astro:content";

const { slug } = Astro.params;

if (slug === undefined) {
  throw new Error("Slug is required");
}

const entry = await getEntry("blog", slug);

if (entry === undefined) {
  return Astro.redirect("/404");
}

// Render the Markdown content into a component
const { Content } = await render(entry);
---

<Layout>
  <MainLayout>
    <a
      href="/articles"
      class="inline-block bg-gray-100 p-2 mb-6 hover:bg-indigo-500 hover:text-white"
      >Back To Articles</a
    >
    <article class="mx-15">
      <h1 class="text-4xl font-bold mb-2">{entry.data.title}</h1>
      <h3 class="text-lg mb-2">
        Written by {entry.data.author} on {formatDate(entry.data.pubDate)}
      </h3>
      <Tags tags={entry.data.tags} />
      <div class="min-w-screen min-h-50 relative">
        <div class="absolute rounded-xl to-0 left-0 w-full h-full bg-zinc-950/20"></div>

        <img
          src={entry.data.image}
          alt="Article Image"
          class="rounded-xl w-full h-150 my-6 object-cover overflow-hidden"
        />
      </div>

      <Content />
    </article>
  </MainLayout>
</Layout>

<style is:global>
  p {
    margin: 20px 0;
  }

  h2 {
    margin: 20px 0;
    font-size: 1.8rem;
  }
</style>

<!--
import MainLayout from "../../layout/MainLayout.astro";
import { getCollection, render } from "astro:content";
import type { CollectionEntry } from "astro:content";
import Layout from "../../layout/layout.astro";

// export async function getStaticPaths() {
//   const allBlogArticles: CollectionEntry<"blog">[] =
//     await getCollection("blog");

//   return allBlogArticles.map((entry) => ({
//     params: {
//       slug: entry.id,
//     },
//     props: { entry },
//   }));
// }

// const { entry } = Astro.props;

// // Render the Markdown content into a component
// const { Content } = await render(entry);

import { getEntry } from "astro:content";
import { formatDate } from "../../utils";
import Tags from "../../components/Tags.astro";

const { slug } = Astro.params;

if (slug === undefined) {
  throw new Error("slug is not defined");
}

const entry = await getEntry("blog", slug);

if (entry === undefined) {
  return Astro.redirect("/404");
}

const { Content } = await render(entry);
-->
